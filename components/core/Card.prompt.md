General-purpose surface container — dashboard widgets, pricing tiles, list items.

```jsx
<Card elevation="sm" interactive>...</Card>
```
`interactive` lifts + deepens shadow on hover — use only when the whole card is clickable.
