A square icon-only button for toolbars, cards, and list rows.

```jsx
<IconButton icon={<TrashIcon/>} variant="ghost" aria-label="Delete"/>
```

Variants: `ghost` (default, transparent), `outline` (bordered), `filled` (solid brand purple, for a single emphasized action like "add"). Always pass `aria-label`.
