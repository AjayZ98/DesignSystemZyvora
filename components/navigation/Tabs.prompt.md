Underline tab bar for settings pages and dashboard section switching.

```jsx
<Tabs tabs={[{value:'overview',label:'Overview'},{value:'billing',label:'Billing'}]} active={tab} onChange={setTab}/>
```
